<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    style="font-family: Helvetica, sans-serif"
    class="w-full min-h-screen bg-gray-900 flex items-center justify-center px-4"
  >
    <main
      class="w-full max-w-3xl bg-gray-50 rounded-md p-4 flex flex-col gap-y-4 items-center"
    >
      <!-- Title -->
      <h2 class="text-xl font-semibold text-gray-900">Login</h2>

      <!-- Login Form -->
      <form
        action="/user/login"
        method="post"
        class="flex flex-col gap-y-3 w-full text-white text-sm"
      >
        <input
          type="email"
          name="email"
          placeholder="Enter your email"
          class="rounded-md block w-full py-2.5 px-3 bg-gray-700 placeholder-gray-300 outline-none"
          required
        />
        <input
          type="password"
          name="password"
          placeholder="Enter your password"
          class="rounded-md block w-full py-2.5 px-3 bg-gray-700 placeholder-gray-300 outline-none"
          required
        />

        <!-- Error Message -->
        <% if(locals.err) {%>
        <p class="text-red-600 text-center font-semibold"><%= err %></p>
        <% } %>

        <button
          type="submit"
          class="bg-gray-800 hover:bg-gray-900 py-2 px-4 rounded-md font-semibold"
        >
          Login
        </button>

        <p class="text-gray-900 text-center">
          Not a member?
          <a href="/signup" class="text-blue-600 font-semibold">Signup</a>
        </p>
      </form>
    </main>
  </body>
</html>
